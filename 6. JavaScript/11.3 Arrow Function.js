// 1. Ida ne Function normal
function elevadu(x){
    return x * x;
}
const rezultadu = elevadu(5);

// 2. Ida ne expression function
const resultadu = function(x){
    return x * x;
}

// 3. Arrow Function
const elevadus = (x) => {
    return x * x;
}

const random_deit = () => {
    return Math.floor(Math.random() * 1000);
}